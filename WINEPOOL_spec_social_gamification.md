# 🍷 WinePool Спецификация Блока 3: Социализация и Геймификация

## 7. "Винная Стена Славы" (The Hall of Fame)

### 7.1 Требования к Бэкенду (API/DB)
* **ТАБЛИЦА:** Создать таблицу `wine_posts`: `post_id`, `user_id`, `tasting_id` (Связь с `user_tastings`), `caption` (Текст поста), `likes_count`, `comments_count`, `created_at`.
* **API ENDPOINT:** `/api/v1/posts/create` (POST). Создание поста, который может быть основан на записи из "Погребка".
* **API ENDPOINT:** `/api/v1/posts/feed`. Лента постов (отсортированная по дате или популярности).
* **API ENDPOINT:** `/api/v1/posts/{post_id}/like` и `/api/v1/posts/{post_id}/comment`.

### 7.2 Требования к Фронтенду (Flutter UI/UX)
* **НАВИГАЦИЯ:** Отдельная вкладка в нижнем меню "Сообщество" или "Лента".
* **ПОСТИНГ:** Кнопка "+" в нижнем меню. При создании поста, пользователь может выбрать запись из своего "Погребка" (`user_tastings`).
* **ИНТЕРФЕЙС ЛЕНТЫ:** Карточки постов, как в социальных сетях: Фото вина, Имя пользователя, Текст, Кнопки "Лайк" и "Комментарий".

---

## 8. Уровни и Бейджи (Геймификация)

### 8.1 Требования к Бэкенду (API/DB)
* **ТАБЛИЦА:** Создать таблицу `user_achievements`: `user_id`, `badge_id`, `unlocked_at`.
* **ТАБЛИЦА:** Создать таблицу `badges`: `badge_id`, `name`, `description`, `icon_url`, `trigger_logic` (описание условия, например, '10_CABERNETS').
* **ЛОГИКА:**
    * **Триггеры:** Бэкенд должен проверять условия после каждой новой записи в `user_tastings` или после покупки.
    * Пример триггера: Если `COUNT(user_tastings WHERE user_id=$X AND wine.variety='Cabernet') > 10`, то выдать бейдж 'Expert Cab'.
* **API ENDPOINT:** `/api/v1/user/badges`. Возвращает список разблокированных бейджей пользователя.

### 8.2 Требования к Фронтенду (Flutter UI/UX)
* **ПРОФИЛЬ ПОЛЬЗОВАТЕЛЯ:** Отдельный раздел "Мои Достижения" или "Бейджи" на странице профиля.
* **ВИЗУАЛИЗАЦИЯ:** Красивые, цветные иконки-бейджи. Визуальный прогресс-бар для бейджей, которые еще не получены (например, "7/10 Шардоне").

---

## 9. "Винные Битвы" (Сравнение коллекций)

### 9.1 Требования к Бэкенду (API/DB)
* **API ENDPOINT:** `/api/v1/cellar/compare?user_b_id={ID}`.
* **ЛОГИКА СРАВНЕНИЯ:** Бэкенд сравнивает ключевые агрегированные метрики двух пользователей (например, среднюю цену вина, распределение по странам, средний рейтинг) и возвращает JSON с результатами:
    ```json
    {
      "metric": "Average Rating",
      "user_a_value": 4.2,
      "user_b_value": 3.9,
      "conclusion": "User A prefers higher rated wines."
    }
    ```

### 9.2 Требования к Фронтенду (Flutter UI/UX)
* **ИНТЕРФЕЙС СРАВНЕНИЯ:** Возможность "Бросить вызов" (Сравнить) другому пользователю со страницы его профиля.
* **ОТОБРАЖЕНИЕ:** Экран, разделенный пополам, где визуально сравниваются Радиальные Диаграммы или Столбчатые Графики двух пользователей, с текстовыми "инсайтами" (`conclusion`).
# 🍷 WinePool Спецификация Блока 1: Уникальный Контент и Стимулы

## 1. Лимитированный Ассортимент (Limited Edition)

### 1.1 Требования к Бэкенду (API/DB)
* **WINE_ENTITY:** Добавить поле `isLimitedEdition` (Boolean) и `limitedQuantity` (Integer).
* **WINE_ENTITY:** Добавить поле `quantityAvailable` (Integer, уменьшается при покупке).
* **API ENDPOINT:** `/api/v1/wines/limited`. Возвращает список лимитированных вин, отсортированных по минимальному `quantityAvailable`.
* **УВЕДОМЛЕНИЯ:** Триггер на бэкенде: если `quantityAvailable` становится $0$ или $< 5$, отправлять уведомление продавцу и администратору.

### 1.2 Требования к Фронтенду (Flutter UI/UX)
* **КАРТОЧКА ТОВАРА:** Яркий, контрастный бейдж "LIMITED EDITION".
* **ОТОБРАЖЕНИЕ:** На карточке товара отображать `Осталось X шт.`. При остатке $< 10$, текст должен быть красным или мигающим.
* **ФИЛЬТРЫ:** Добавить опцию фильтрации "Показать только лимитированные вина".

---

## 2. Ежедневный Винный Индекс (The WinePool Index)

### 2.1 Требования к Бэкенду (API/DB)
* **ТАБЛИЦА:** Создать отдельную таблицу `daily_wine_index` с полями: `date`, `indexName` (e.g., 'Sauvignon Index'), `indexValue` (e.g., '14.5%'), `trend` ('UP'/'DOWN'). Обновляется **ежедневно в 00:00 UTC**.
* **ЛОГИКА:** Индекс должен рассчитываться на основе данных за последние 24/48 часов.
    * **"Индекс Совиньона"**: Процент от общего объема проданных вин, которые относятся к этому сорту (или самому популярному сорту).
    * **"Индекс Доступности"**: Средняя цена всех **купленных** бутылок.
* **API ENDPOINT:** `/api/v1/index/daily`. Возвращает 3-5 актуальных показателей Индекса.

### 2.2 Требования к Фронтенду (Flutter UI/UX)
* **ГЛАВНЫЙ ЭКРАН (Dashboard):** Разместить блок Индекса в верхней трети экрана, чтобы его было видно сразу.
* **ВИЗУАЛИЗАЦИЯ:** Для каждого показателя использовать крупный шрифт, цветной индикатор (красный/зеленый) и стрелку, показывающую тренд (вверх/вниз) по сравнению со вчерашним днем.

---

## 3. "Винный Охотник" (Заявки на Поиск)

### 3.1 Требования к Бэкенду (API/DB)
* **ТАБЛИЦА:** Создать таблицу `wine_requests`: `user_id`, `wine_name_requested`, `vintage_requested` (nullable), `status` ('OPEN', 'FULFILLED'), `created_at`.
* **API ENDPOINT:** `/api/v1/requests/create` (POST). Создает заявку.
* **API ENDPOINT (Продавцы):** `/api/v1/seller/requests`. Отображает список открытых заявок для продавцов.
* **ЛОГИКА СВЯЗИ:** Когда продавец добавляет вино с названием, соответствующим открытой заявке (`status='OPEN'`), статус заявки автоматически переключается на `FULFILLED`, и генерируется **Push-уведомление** пользователю.

### 3.2 Требования к Фронтенду (Flutter UI/UX)
* **ПОЛЬЗОВАТЕЛЬ:** Отдельный пункт меню/раздел "Я ищу это вино" с формой ввода.
* **УВЕДОМЛЕНИЯ:** При получении уведомления, пользователь должен по клику попадать на карточку добавленного товара.
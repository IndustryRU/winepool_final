# 🍷 WinePool Спецификация Блока 2: Персональная Коллекция и Аналитика (Погребок)

## 4. Интерактивный Погребок (My Cellar)

### 4.1 Требования к Бэкенду (API/DB)
* **ТАБЛИЦА:** Создать ключевую таблицу `user_tastings` (для "Пробовал") с полями:
    * `user_id`
    * `wine_id` (Связь с основной таблицей `WINE_ENTITY`)
    * `tasting_date` (Дата употребления/дегустации)
    * `rating` (Integer, 1-5 звезд)
    * `notes` (Text, личные заметки)
    * `photo_attachment` (URL, опциональное фото)
* **ТАБЛИЦА:** Создать таблицу `user_storage` (для "Храню"): `user_id`, `wine_id`, `quantity`, `purchase_price` (опционально), `ideal_drink_year` (опционально).
* **API ENDPOINT:** `/api/v1/cellar/tastings` (POST, GET, DELETE). Управление записями о дегустациях.
* **ЛОГИКА:** При покупке вина через WinePool, оно автоматически попадает в `user_storage`.

### 4.2 Требования к Фронтенду (Flutter UI/UX)
* **НАВИГАЦИЯ:** Добавить вкладку или отдельный раздел "Мой Погребок".
* **ИНТЕРФЕЙС:** Внутри Погребка - переключатель (Segmented Control) между "Пробовал" и "Храню".
* **ФОРМА ДОБАВЛЕНИЯ:** Упрощенная форма ввода: **Вино** (через поиск по каталогу или сканирование штрихкода), **Дата**, **Оценка** (звезды/слайдер), **Заметки**.
* **КАРТОЧКА ВИНА:** На карточке любого вина в каталоге должна быть кнопка: "Добавить в Погребок" (выбор: Пробовал/Храню).

---

## 5. Визуальная Аналитика Дегустаций

### 5.1 Требования к Бэкенду (API/DB)
* **API ENDPOINT:** `/api/v1/cellar/analytics`. Агрегация данных из `user_tastings` по `user_id`.
* **АГРЕГАЦИЯ:** Расчет следующих показателей:
    * Топ-5 сортов (по количеству записей).
    * Топ-3 страны/региона.
    * Средняя оценка.
    * Диапазон цен (по `purchase_price` или средней цене вина).
    * **Паутина Вкуса (JSON Array):** Соотношение показателей, необходимых для построения радиальной диаграммы (сладость, кислотность, танины, полнотелость — если эти поля есть в `WINE_ENTITY`).

### 5.2 Требования к Фронтенду (Flutter UI/UX)
* **РАЗДЕЛ:** Отдельная вкладка "Аналитика" внутри "Погребка".
* **ВИЗУАЛИЗАЦИЯ:**
    * Использование библиотеки для построения **Радиальных Диаграмм** (например, `syncfusion_flutter_charts`).
    * Крупные, понятные графики (круговые, столбчатые) для отображения разбивки по сортам и регионам.
    * Визуальный "счетчик" личных достижений (Средний рейтинг $X.X$).